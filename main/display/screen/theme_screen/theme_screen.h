#pragma once

#include "lvgl.h"
#include "screen_util.h"

// ---------------------------------------------------------------------------
// ThemeScreen
//
// 主题选择 App。布局按当前显示屏原生宽高计算：
//
//   ┌────────────────────────────────────────────────────────┐
//   │  [←]    主题                                            │  ← header 90px
//   ├────────────────────────────────────────────────────────┤
//   │                                                        │
//   │      ┌──────┐         ┌──────┐                         │
//   │      │ T1   │         │ T2   │  ← 180x180 预览卡片      │
//   │      └──────┘         └──────┘                         │
//   │      主题 1            主题 2                           │
//   │                                                        │
//   │      ┌──────┐         ┌──────┐                         │
//   │      │ T3   │         │ T4   │                         │
//   │      └──────┘         └──────┘                         │
//   │      主题 3            主题 4                           │
//   │                                                        │
//   │  切换后将立即应用，并返回主页查看新图标                       │
//   └────────────────────────────────────────────────────────┘
//
// 交互：
//   - 点击非当前主题 -> 弹模态对话框 "切换 / 取消"
//   - 点击当前主题 -> no-op
//   - 确认切换 -> ThemeManager::SetCurrentThemeId() 写 NVS，
//     重建 HomeScreen 并跳转，主屏图标立即按新主题加载
// ---------------------------------------------------------------------------
class ThemeScreen {
public:
    static lv_obj_t* Create();
    static void LifecycleCallback(screen_lifecycle_event_t event);
};
